import { StyleSheet } from "react-native";

// Vá até o final da palavra contendo o componente e aperte CTRL + Espaço
const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#fff",
        alignItems: "center",
        justifyContent: "center",
      },
      titulo1: {
        fontSize: 18,
        fontWeight: "bold",
        padding: 20
      },
});

export default styles;